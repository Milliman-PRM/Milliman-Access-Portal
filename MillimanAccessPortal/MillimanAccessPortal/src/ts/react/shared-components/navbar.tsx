import '../../../scss/react/shared-components/navbar.scss';

import * as React from 'react';

import { NavBarProps, NavBarState } from './interfaces';

export class ActionIcon extends React.Component<NavBarProps, NavBarState> {
  public render() {
    return (
      <nav>
      </nav>
    );
  }
}
